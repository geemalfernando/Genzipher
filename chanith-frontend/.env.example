# Local dev (recommended): keep proxy + avoid CORS
VITE_API_BASE=http://localhost:3001/api

# Production (Firebase hosting): set this before `npm run build`
# VITE_API_BASE=https://genzipher.vercel.app/api
